---
dependencies:
  - { role: goozbach.EPEL, when: ansible_os_family == "RedHat" }

galaxy_info:
  author: gabethecabbage
  description: Ensures security standards are applied to the SSH server
  license: BSD
  min_ansible_version: 2.3
  platforms:
    - name: Ubuntu
      versions:
        - xenial
        - yakkety
        - zesty
        - artful
    - name: EL
      versions:
        - 6
        - 7
  galaxy_tags:
    - networking
    - system
